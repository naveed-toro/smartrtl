{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run SmartRTL for Claude Code",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/apps/vscode-claude"],
      "preLaunchTask": "build payload"
    }
  ]
}
