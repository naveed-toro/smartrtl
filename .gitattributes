# One line ending everywhere in the repository, whichever machine commits.
# Windows, macOS and Linux all check out what suits them; the stored form stays LF.
* text=auto eol=lf

# Never let anything rewrite these
*.png binary
*.jpg binary
*.vsix binary
*.ico binary
